Proyecto para Challenge de MercadoLibre

## Comenzando

Primero, instalar dependencias:

```bash
$ yarn
# o
$ npm i
```

A continuación, iniciar proyecto:

```bash
$ yarn dev
# o
$ npm run dev
```

Por último, visualizar la web en `http://localhost:3000/`
